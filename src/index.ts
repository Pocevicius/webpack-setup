import getFullAvatars from "./getFullAvatars";
import "./styles.sass"
console.log("working");
getFullAvatars();

const first = { a: 1, b: 2 };
const second = { ...first, c: 3 };

console.log(second);
